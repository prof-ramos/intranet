{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/.mcp.json",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "${DATABASE_MIGRATION_URL:-}"]
    }
  }
}
